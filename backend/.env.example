# Copie para .env e preencha. O arquivo .env nao vai para o git.

DB_USER=postgres
DB_PASS=sua_senha
DB_HOST=localhost
DB_PORT=5432
DB_NAME=vestibulif_api

# Assine os JWT com um segredo proprio. Para gerar um:
#   python -c "import secrets; print(secrets.token_urlsafe(48))"
SECRET_KEY=troque_por_um_segredo_longo_e_aleatorio
ALGORITHM=HS256
